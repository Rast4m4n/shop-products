import 'package:flutter/cupertino.dart';

class MainShopView extends StatelessWidget {
  const MainShopView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
